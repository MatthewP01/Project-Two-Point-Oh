
<h1>Market</h1>
<div class="card-columns">
<% @fish.each do |fish|%>
	<div class="card">
	  <div class="card-body">
	  	<%= fish.name %><br>
	  	<div class="fishpic">
	  		<%= image_tag "#{fish.id}.png", size: "150x150", alt: "#{fish.name}" %>
	  	</div>
	  </div>
	</div>
<% end %> 
</div>